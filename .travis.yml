language: php

matrix:
  fast_finish: true
  include:
    - php: 7.1
    - php: 7.2
    - php: nightly
  allow_failures:
    - php: nightly

before_script:
  - composer install
  - npm i
  - npm run build

script:
  - composer test
