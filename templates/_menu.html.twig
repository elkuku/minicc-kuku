{% import "_macro/g11n.html.twig" as g11n %}
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('welcome') }}">MiniCC KuKu</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <div class="navbar-nav">
            <ul class="nav navbar-nav">

                <li class="nav-item"><a class="nav-link" href="{{ path('about') }}">{{ _('About') }}</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('contact') }}">{{ _('Contact') }}</a></li>

                {% if app.user and app.user.role == 'ROLE_ADMIN' %}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button">Admin</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('pay-day') }}">Pay Day</a>
                            <a class="dropdown-item" href="{{ path('cobrar') }}">Cobrar Alquileres</a>
                            <a class="dropdown-item" href="{{ path('pagos-por-ano') }}">Pagos por Año</a>
                            <a class="dropdown-item" href="{{ path('admin-tasks') }}">Tasks</a>
                            <div class="dropdown-divider"></div>
                            <h6 class="dropdown-header">etc</h6>
                            <a class="dropdown-item" href="{{ path('transaction-rawlist') }}">Transactions RAW</a>
                            <a class="dropdown-item" href="{{ path('deposits') }}">Depositos</a>
                            <div class="dropdown-divider"></div>
                            <h6 class="dropdown-header">System</h6>
                            <a class="dropdown-item" href="{{ path('sysinfo') }}">Info</a>
                        </div>
                    </li>
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button">{{ _('Setup') }}</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('stores-list') }}">{{ _('Stores') }}</a>
                            <a class="dropdown-item" href="{{ path('users-list') }}">{{ _('Clients') }}</a>
                            <a class="dropdown-item" href="{{ path('contract-list') }}">{{ _('Contracts') }}</a>
                            <div class="dropdown-divider"></div>
                            <h6 class="dropdown-header">Params</h6>
                            <a class="dropdown-item" href="{{ path('payment-methods') }}">{{ _('Payment Methods') }}</a>
                        </div>
                    </li>
                {% endif %}

            </ul>
        </div>

        <div class="ml-auto">
            {% if app.user %}
                <a class="nav-link" accesskey="l" href="{{ path('logout') }}" title="{{ _('Logout') }}">
                    <span class="oi oi-account-logout"></span>
                </a>
            {% else %}
                <a class="nav-link" accesskey="l" href="{{ path('login') }}" title="{{ _('Login') }}">
                    <span class="oi oi-account-login"></span>
                </a>
            {% endif %}
        </div>

        {{ g11n.switcher(getLangs(), getCurrentLang()) }}

    </div>
</nav>
